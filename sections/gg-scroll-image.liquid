<!-- GG-首页-滚动品牌图 -->
<!-- 品牌轮播条 -->

<link rel="stylesheet" href="{{ 'gg-scroll-image.css' | asset_url }}">
{% comment %} <noscript>{{ 'gg-scroll-image.css' | asset_url | stylesheet_tag }}</noscript> {% endcomment %}

{%- style -%}
.scroll-image-wrapper {
  --scroll-speed: {{ section.settings.scroll_speed }}s;
  --gap: {{ section.settings.gap }}px;
}
{%- endstyle -%}

<div class="scroll-image-wrapper">
  <div class="scroll-image-container">
    <div class="scroll-image-track" data-track="first">
      {%- for block in section.blocks -%}
        <div class="scroll-image-item" {{ block.shopify_attributes }}>
          <!-- 桌面端图片 -->
          {%- if block.settings.image != blank -%}
            {{ block.settings.image | image_url: width: 1920 | image_tag: 
              class: 'scroll-image desktop-only',
              loading: 'lazy',
              width: block.settings.image.width,
              height: block.settings.image.height,
              sizes: '100vw'
            }}
          {%- endif -%}
          
          <!-- 移动端图片 -->
          {%- if block.settings.mobile_image != blank -%}
            {{ block.settings.mobile_image | image_url: width: 1200 | image_tag: 
              class: 'scroll-image mobile-only',
              loading: 'lazy',
              width: block.settings.mobile_image.width,
              height: block.settings.mobile_image.height,
              sizes: '100vw'
            }}
          {%- else -%}
            <!-- 如果没有移动端图片，使用桌面图片 -->
            {%- if block.settings.image != blank -%}
              {{ block.settings.image | image_url: width: 1200 | image_tag: 
                class: 'scroll-image mobile-only',
                loading: 'lazy',
                width: block.settings.image.width,
                height: block.settings.image.height,
                sizes: '100vw'
              }}
            {%- endif -%}
          {%- endif -%}
          
          {%- if block.settings.image == blank and block.settings.mobile_image == blank -%}
            {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
    <div class="scroll-image-track" data-track="second">
      {%- for block in section.blocks -%}
        <div class="scroll-image-item" {{ block.shopify_attributes }}>
          <!-- 桌面端图片 -->
          {%- if block.settings.image != blank -%}
            {{ block.settings.image | image_url: width: 1920 | image_tag: 
              class: 'scroll-image desktop-only',
              loading: 'lazy',
              width: block.settings.image.width,
              height: block.settings.image.height,
              sizes: '100vw'
            }}
          {%- endif -%}
          
          <!-- 移动端图片 -->
          {%- if block.settings.mobile_image != blank -%}
            {{ block.settings.mobile_image | image_url: width: 1200 | image_tag: 
              class: 'scroll-image mobile-only',
              loading: 'lazy',
              width: block.settings.mobile_image.width,
              height: block.settings.mobile_image.height,
              sizes: '100vw'
            }}
          {%- else -%}
            <!-- 如果没有移动端图片，使用桌面图片 -->
            {%- if block.settings.image != blank -%}
              {{ block.settings.image | image_url: width: 1200 | image_tag: 
                class: 'scroll-image mobile-only',
                loading: 'lazy',
                width: block.settings.image.width,
                height: block.settings.image.height,
                sizes: '100vw'
              }}
            {%- endif -%}
          {%- endif -%}
          
          {%- if block.settings.image == blank and block.settings.mobile_image == blank -%}
            {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
    <div class="scroll-image-track" data-track="third">
      {%- for block in section.blocks -%}
        <div class="scroll-image-item" {{ block.shopify_attributes }}>
          <!-- 桌面端图片 -->
          {%- if block.settings.image != blank -%}
            {{ block.settings.image | image_url: width: 1920 | image_tag: 
              class: 'scroll-image desktop-only',
              loading: 'lazy',
              width: block.settings.image.width,
              height: block.settings.image.height,
              sizes: '100vw'
            }}
          {%- endif -%}
          
          <!-- 移动端图片 -->
          {%- if block.settings.mobile_image != blank -%}
            {{ block.settings.mobile_image | image_url: width: 1200 | image_tag: 
              class: 'scroll-image mobile-only',
              loading: 'lazy',
              width: block.settings.mobile_image.width,
              height: block.settings.mobile_image.height,
              sizes: '100vw'
            }}
          {%- else -%}
            <!-- 如果没有移动端图片，使用桌面图片 -->
            {%- if block.settings.image != blank -%}
              {{ block.settings.image | image_url: width: 1200 | image_tag: 
                class: 'scroll-image mobile-only',
                loading: 'lazy',
                width: block.settings.image.width,
                height: block.settings.image.height,
                sizes: '100vw'
              }}
            {%- endif -%}
          {%- endif -%}
          
          {%- if block.settings.image == blank and block.settings.mobile_image == blank -%}
            {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "GG-品牌轮播组件",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "range",
      "id": "scroll_speed",
      "min": 10,
      "max": 60,
      "step": 5,
      "unit": "s",
      "label": "滚动速度",
      "default": 30
    },
    {
      "type": "range", 
      "id": "gap",
      "min": 10,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "图片间距",
      "default": 30
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "品牌图片",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "品牌图片，建议尺寸：1920px * 91px"
        },
        {
          "type": "image_picker",
          "id": "mobile_image",
          "label": "移动端品牌图片，建议尺寸：1228px * 58px"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "GG-品牌轮播组件"
    }
  ]
}
{% endschema %} 